name: beyondtranslate_ui
description: "The Flutter implementation of the BeyondTranslate UI design system."
publish_to: "none"
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  collection: ^1.18.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
